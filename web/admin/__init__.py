"""Admin panel: bot configuration, logs, and conversation transcripts.

Gated by Basic Auth (see auth.py) on every route in this package — the
`/admin` page itself and every `/admin/api/*` endpoint.
"""
